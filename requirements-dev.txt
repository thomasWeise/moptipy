# We need pytest to run the tests.
pytest >= 6.2.2

# setuptools, testresources, and wheel are needed for generating the
# installable pip package.
setuptools >= 53.0
testresources >= 2.0.1
wheel >= 0.36.2

# sphinx is needed for generating the documentation.
sphinx >= 3.4.3

# static code analysis: flake8, pylint, and mypy
flake8 >= 3.8.3
pylint >= 2.7.2
mypy >= 0.812

# yaml is currently used only in tests and examples and therefore not
# strictly needed. However, it may be needed later on, namely when adding
# components for parsing the log files.
pyyaml >= 5.3.1